# path|token|evidence (evidence is mandatory and is what a reviewer reads)
#
# Phantom-region hits only (decision 1413, 2026-09-13): docs/canon/data-
# residency.md's "The truth" section (git 37a15d6d, the LATER, explicitly-
# titled "CORRECT DR-1 model" correction) says the mobile REGIONS array
# (europe + falkenstein-de available:true, helsinki-fi + ede-nl available:
# false) "is correct — leave it." Helsinki and Ede are real, committed future
# regions, clearly marked not-yet-available: `available: false` renders a
# "Soon" badge, and the row's subtitle shows "Coming soon" (not the
# selectable-only "Preference or force" copy — SettingsScreen.tsx's
# `{r.available ? r.subtitle : 'Coming soon'}` render, fixed alongside this
# file per a Codex review that correctly caught the evidence not matching
# the code at the time) — never implying either stores data or is selectable
# today. This file does NOT clear any provider-name/capability-claim hit
# (none exist in mobile src today; per decision 1413 those would still get
# fixed, never allow-listed).
#
# UPDATE (task 1420): marketing/mockups.html — the App Store design mockup
# the note above left untouched — was DELETED, not allow-listed. `git grep
# mockups.html` across the tracked tree found no reference to it anywhere
# (not from app code, not from any build/marketing tooling); it was dead
# weight. Deleting it also clears its 3 banned-origin (Google Fonts
# preconnect/import) + 3 host-allowlist (same hosts) + 1 phantom-region
# (Helsinki) + 1 region-claim ("Choose your region") findings.
src/screens/SettingsScreen.tsx|helsinki|docs/canon/data-residency.md "The truth" (37a15d6d) + decision 1413 — REGIONS entry for a real, clearly-marked coming-soon region (available: false)
src/screens/SettingsScreen.tsx|Helsinki|docs/canon/data-residency.md "The truth" (37a15d6d) + decision 1413 — REGIONS entry for a real, clearly-marked coming-soon region (available: false)
src/screens/SettingsScreen.tsx|'ede-|docs/canon/data-residency.md "The truth" (37a15d6d) + decision 1413 — REGIONS entry for a real, clearly-marked coming-soon region (available: false)
src/screens/SettingsScreen.tsx|'Ede'|docs/canon/data-residency.md "The truth" (37a15d6d) + decision 1413 — REGIONS entry for a real, clearly-marked coming-soon region (available: false)

# ── host-allowlist (task 1420) ──────────────────────────────────────────────
# Our own public GitHub org — same reasoning already accepted for site
# (repos/site/.claims-allow: "our own public beebeeb-io org/repos").
modules/beebeeb-crypto/ios/BeebeebCrypto.podspec|github.com|podspec `s.source` git URL — our own public beebeeb-io/mobile repo, build-time only
src/screens/SettingsScreen.tsx|github.com|"View source" link to our own public beebeeb-io/mobile repo (open-source disclosure)

# ── banned-claim / phantom-region (base64-encoded image noise) ─────────────
# marketing/app-store/*/03-back-up-memories.svg embeds a raster screenshot as
# an inline `data:image/png;base64,...` payload. The base64 alphabet
# coincidentally contains the substrings "Soc2" (BANNED_CLAIMS "SOC ?2") and
# "+edE+" (PHANTOM_REGIONS "ede") purely by chance inside the encoded image
# bytes — verified by reading the file: every actual `<text>` element (the
# real App Store screenshot captions, e.g. "Back up your memories") is clean
# of both. Same (file, token) pair reproduces identically across all four
# device sizes because they're the same source photo re-exported at each
# canvas size through the same pipeline.
marketing/app-store/ipad-13/03-back-up-memories.svg|Soc2|coincidental match inside base64-encoded PNG image data, not text copy — the file's real <text> captions are clean (verified by reading them)
marketing/app-store/iphone-5.5/03-back-up-memories.svg|Soc2|same — coincidental base64 match, real <text> captions clean
marketing/app-store/iphone-6.7/03-back-up-memories.svg|Soc2|same — coincidental base64 match, real <text> captions clean
marketing/app-store/iphone-6.9/03-back-up-memories.svg|Soc2|same — coincidental base64 match, real <text> captions clean
marketing/app-store/ipad-13/03-back-up-memories.svg|+edE+|coincidental match inside base64-encoded PNG image data, not a region claim — the file's real <text> captions are clean (verified by reading them)
marketing/app-store/iphone-5.5/03-back-up-memories.svg|+edE+|same — coincidental base64 match, real <text> captions clean
marketing/app-store/iphone-6.7/03-back-up-memories.svg|+edE+|same — coincidental base64 match, real <text> captions clean
marketing/app-store/iphone-6.9/03-back-up-memories.svg|+edE+|same — coincidental base64 match, real <text> captions clean
